(function(){ function createHeart(x,y){ const el=document.createElement('div'); el.textContent=Math.random()>0.4?'💜':'🧡'; el.style.position='fixed'; el.style.left=x+'px'; el.style.top=y+'px'; el.style.pointerEvents='none'; el.style.zIndex=9999; const size=16+Math.random()*16; el.style.fontSize=size+'px'; const dx=(Math.random()*2-1)*40, dy=80+Math.random()*60, rot=(Math.random()*2-1)*30, dur=800+Math.random()*600; const start=performance.now(); function anim(t){ const p=Math.min(1,(t-start)/dur); const ease=1-Math.pow(1-p,2); el.style.transform=`translate(${dx*ease}px, ${-dy*ease}px) rotate(${rot*ease}deg)`; el.style.opacity=(1-p).toFixed(2); if(p<1) requestAnimationFrame(anim); else el.remove(); } document.body.appendChild(el); requestAnimationFrame(anim);} function handler(e){ const x=(e.touches?e.touches[0].clientX:e.clientX)||0; const y=(e.touches?e.touches[0].clientY:e.clientY)||0; const count=6+Math.floor(Math.random()*5); for(let i=0;i<count;i++) setTimeout(()=>createHeart(x,y), i*40);} document.addEventListener('click',handler); document.addEventListener('touchstart',handler,{passive:true}); })();
