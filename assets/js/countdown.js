(function(){ const el=document.getElementById('countdown'); if(!el) return; function target(){ const now=new Date(); const y=now.getFullYear(); const t=new Date(y,1,14,0,0,0); return now>t? new Date(y+1,1,14,0,0,0): t; } const T=target(); const pad=n=>String(n).padStart(2,'0'); function render(){ const now=new Date(); let diff=T-now; if(diff<0) diff=0; const d=Math.floor(diff/86400000); const h=Math.floor((diff%86400000)/3600000); const m=Math.floor((diff%3600000)/60000); const s=Math.floor((diff%60000)/1000); el.innerHTML=`<div class="cd-item"><span>${pad(d)}</span><small>días</small></div><div class="cd-sep">:</div><div class="cd-item"><span>${pad(h)}</span><small>horas</small></div><div class="cd-sep">:</div><div class="cd-item"><span>${pad(m)}</span><small>min</small></div><div class="cd-sep">:</div><div class="cd-item"><span>${pad(s)}</span><small>seg</small></div>`; } render(); setInterval(render,1000);})();
